{% block body %}
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
	<a class="navbar-brand p-1" href="{{ path('app_home') }}">Video Center</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>
	<div class="collapse navbar-collapse" id="navbarsExample04">
		<ul class="navbar-nav me-auto mb-2 mb-md-0">
			{% if app.user %}
				<a class="nav-item nav-link" href="{{ path('app_video_create') }}">Create Video</a>
				<a class="nav-item nav-link" href="#">Account</a>
				<a class="nav-item nav-link" href="{{ path('app_logout') }}">Logout</a>
			{% else %}
				<a class="nav-item nav-link" href="{{ path('app_register') }}">Register</a>
				<a class="nav-item nav-link" href="{{ path('app_login') }}">Login</a>
			{% endif %}
		</div>
	</div>
</nav>
{% endblock %}